{
  "name": "{{PROJECT_SLUG}}",
  "version": "{{VERSION}}",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --format=esm --outfile=dist/ui.js",
    "dev": "esbuild src/index.tsx --bundle --format=esm --outfile=dist/ui.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.7.0"
  }
}
