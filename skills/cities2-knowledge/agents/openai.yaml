interface:
  display_name: "Cities2 Knowledge"
  short_description: "Answer gameplay questions from CS2 sources"
  default_prompt: "Use $cities2-knowledge to answer a Cities: Skylines II gameplay question from the wiki and local Encyclopedia."

dependencies:
  tools:
    - type: "mcp"
      value: "cities2-mcp"
      description: "Cities2-MCP local server with wiki corpus and game Encyclopedia tools"
