{
  "mcpServers": {
    "cities2-mcp": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/bin/cities2-mcp-launcher.js",
        "--workspace",
        "${CLAUDE_PROJECT_DIR}"
      ]
    }
  }
}
